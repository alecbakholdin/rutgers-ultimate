import React from "react";
import TextDisplay from "appComponents/textDisplay/TextDisplay";

export default function NotFound(): React.ReactElement {
  return <TextDisplay text={"This event does not exist."} />;
}
